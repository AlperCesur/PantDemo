@model LoginRegistrationInMVCWithDatabase.ViewModel.LoginViewModel
@{
    ViewBag.Title = "Login";

}

<h2>Login Page</h2>


<div>
    @using (Html.BeginForm("Login", "Account"))
    {
        <div style="color:red;">@Html.ValidationSummary()</div>
        <div class="row">
            <!--Show details are saved successfully message-->
            <div class="col-lg-12">@ViewBag.Message</div>
        </div><br />

        <div class="row">
            <div class="col-lg-2">@Html.LabelFor(a => a.Username)</div>
            <div class="col-lg-10">@Html.TextBoxFor(a => a.Username, new { @class = "form-control" })</div>
        </div><br />
        <div class="row">
            <div class="col-lg-2">@Html.LabelFor(a => a.Password)</div>
            <div class="col-lg-10">@Html.TextBoxFor(a => a.Password, new { @class = "form-control", type = "Password" })</div>
        </div><br />
        <div class="row">
            <div class="col-lg-2" style="width:300px">@Html.ActionLink("Go to Registration Page", "Register", "Account",new {type = "link"})</div>
        </div><br />
        <input type="submit" value="Login" class="btn btn-primary" />
    }

</div>